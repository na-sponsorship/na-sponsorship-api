{
  "routes": [
    {
      "method": "GET",
      "path": "/sponsorships",
      "handler": "Sponsorship.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/sponsorships/count",
      "handler": "Sponsorship.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/sponsorships/:_id",
      "handler": "Sponsorship.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/sponsorships",
      "handler": "Sponsorship.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/sponsorships/:_id",
      "handler": "Sponsorship.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/sponsorships/:_id",
      "handler": "Sponsorship.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
